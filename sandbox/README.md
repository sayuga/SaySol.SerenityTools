# SaySol Serenity Disposable Sandbox

Canonical clean control host for capsule validation. The committed baseline
contains no SaySol capsules. This commit is a candidate scaffold, not a
validated Serene-generated baseline: generation and runtime validation require
the .NET 10 gate. See the Sandbox Testing Guide in the repository wiki.

Lifecycle: BASELINE -> VALIDATE -> INSTALL -> BUILD -> TEST -> VALIDATE CAPSULE
-> CAPTURE EVIDENCE -> DISPOSE.
