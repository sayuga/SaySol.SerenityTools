# Admin Configuration installation prompt

Installation is blocked while the capsule is `implemented`. Do not create a
settings table, expose connection strings, edit startup, or add navigation by
approximation. Those operations require verified lane-specific manifest entries.

When verified, require the root import workflow, the `Administration:Configuration`
permission, server-side authorization, optimistic revision checks, and a human
gate for environment/database profile changes.
